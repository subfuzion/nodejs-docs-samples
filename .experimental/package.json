{
  "name": "mc",
  "description": "run sample(s) and report exit codes",
  "version": "1.0.0",
  "private": true,
  "author": "Google LLC",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "bin": {
    "mc": "bin/main.ts"
  },
  "imports": {
    "#lib/*": "./lib/*",
    "#test/*": "./test/*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types --disable-warning=ExperimentalWarning ./lib/**/*.test.ts",
    "check": "biome format --write && tsc"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.2",
    "@types/node": "22.5.5",
    "typescript": "5.6.2"
  }
}
